# docker-compose.override.ci.yml
services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    image: local-backend
    ports:
      - "8000:8000"  # пробрасываем порт

  frontend:
    build:
      context: ./frontend
    image: local-frontend
